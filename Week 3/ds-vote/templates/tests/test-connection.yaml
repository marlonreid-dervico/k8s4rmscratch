apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ds-vote.fullname" . }}-test-connection"
  labels:
    {{- include "ds-vote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ds-vote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
