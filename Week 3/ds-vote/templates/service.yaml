apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-vote.fullname" . }}
  labels:
    {{- include "ds-vote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-vote.selectorLabels" . | nindent 4 }}
