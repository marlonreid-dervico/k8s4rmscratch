apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-db.fullname" . }}
  labels:
    {{- include "ds-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-db.selectorLabels" . | nindent 4 }}
