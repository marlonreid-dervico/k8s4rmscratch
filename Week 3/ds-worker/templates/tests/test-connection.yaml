apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ds-worker.fullname" . }}-test-connection"
  labels:
    {{- include "ds-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ds-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
