apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-worker.fullname" . }}
  labels:
    {{- include "ds-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-worker.selectorLabels" . | nindent 4 }}
