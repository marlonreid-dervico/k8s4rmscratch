apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-nats.fullname" . }}
  labels:
    {{- include "ds-nats.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-nats.selectorLabels" . | nindent 4 }}
